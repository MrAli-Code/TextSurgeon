# Normalize line endings: LF in the repo and on checkout…
* text=auto eol=lf

# …except the Windows launcher, which must stay CRLF to be double-clickable
*.bat text eol=crlf

# Binary assets
*.jpg binary
*.png binary
